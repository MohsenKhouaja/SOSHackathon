export function AlphaNoticeDialog() {
  // Placeholder: this dialog was referenced from the layout but the implementation
  // wasn't checked in. Keeping it as a no-op avoids breaking builds.
  return null;
}
